<UserControl x:Class="CerrarAplicacion" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           d:DesignWidth="640" d:DesignHeight="300"
           >
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock x:Name="txtTitulo" Style="{StaticResource PaginasTitulo}" Text ="Consola" />
            <TextBlock x:Name="txtMsg" Style="{StaticResource PaginasSubtitulo}" Text ="Reinicie la aplicación para que los cambios se vean reflejados" />
        </StackPanel>
    </Grid>
</UserControl>

