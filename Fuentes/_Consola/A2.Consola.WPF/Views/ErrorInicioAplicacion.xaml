<UserControl x:Class="ErrorInicioAplicacion" 
           xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
           xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
           xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
           xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
           mc:Ignorable="d"
           d:DesignWidth="640" d:DesignHeight="300"
           >
    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="300"/>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Vertical" VerticalAlignment="Center">
            <TextBlock x:Name="txtTitulo" Style="{StaticResource PaginasTitulo}" Text ="Consola" />
            <TextBlock x:Name="txtMsg" Style="{StaticResource PaginasSubtitulo}" Text ="La aplicación no puede ser inicializada" />
            <TextBlock x:Name="txtMsgSep" Style="{StaticResource PaginasComentario}" Text =" " />
            <TextBlock x:Name="txtMsgDet" Style="{StaticResource PaginasComentario}" Text ="Reinicie la aplicación y si el problema persiste por favor comuniquese con el administrador del sistema" />
        </StackPanel>
    </Grid>
</UserControl>

